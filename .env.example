# Copy to .env.testing. The suite DROPs and recreates DB_DATABASE on every run, so point it
# at a scratch database -- never at a tenant's.
DB_HOST=127.0.0.1
DB_PORT=3308
DB_DATABASE=interadmin_orm_test
DB_USERNAME=root
DB_PASSWORD=
